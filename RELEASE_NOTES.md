# InputBeacon 1.4.3 · 更快的输入框切换提示

从浏览器搜索框切到微信输入框时，取消额外的 200 ms 等待：首次读到有效光标和输入状态就显示当前中/英与 A/a，并按设置的秒数消失。

- 跨应用 / 原生输入控件切换，在首次有效采样时立即触发。
- 共享同一窗口的虚拟输入框改为下一次轮询确认，通常额外约 100 ms，保留对单次控件标识波动的过滤。
- 中英文状态变化仍保留至少 200 ms 的稳定性确认，避免输入法状态波动误弹；大小写变化提示保持不变。
- 同一输入框内打字、移动光标不延长倒计时；光标临时丢失、未知状态恢复和全角标记变化不重复触发。
- 倒计时从确认显示起计算完整时长。实际响应仍取决于轮询时机和目标应用提供光标的速度。

下载 **[InputBeacon.exe](https://github.com/LiguoXia/InputBeacon/releases/download/v1.4.3/InputBeacon.exe)** 双击运行，或下载 **[便携 ZIP](https://github.com/LiguoXia/InputBeacon/releases/download/v1.4.3/InputBeacon-v1.4.3-portable.zip)**。ZIP 内含相同程序 `键盘状态.exe`、中文使用说明和版本记录。

**升级：**先退出旧版再运行新版，已有颜色、显示方式和时长设置保留。发布的 EXE 与本地 1.4.3 验证版完全相同，已运行该验证版的用户无需重新替换程序。

**验证：**通过 176 项自动检查，包括首次采样立即提示、虚拟控件下一轮确认、完整倒计时、防误触发，以及真实 Windows 文本框的 UIA 标识。不同浏览器、微信版本和自绘控件的兼容性取决于它们提供的光标接口；不提供独立控件标识时，同一窗口内的输入框切换可能无法区分。

需要 Windows 10 / 11 和 .NET Framework 4.8。程序未签名，校验值见 `SHA256SUMS.txt`。IDEA / Java 桥接设置及详细用法见 README。
